export function SiteFooter() {
  return (
    <footer className="mt-16 border-t border-[var(--ink)]/10 pt-6 text-xs uppercase tracking-[0.3em] text-[var(--ink)]/40">
      Built for artist submissions and label review.
    </footer>
  );
}
